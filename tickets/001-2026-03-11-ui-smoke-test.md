# Ticket 001 (2026-03-11) — UI Smoke Test + Menu/Button Verification

## Requirements
- Run the full UI smoke test checklist: launch app, open/close project, run core flows, verify menus, verify button interactions, exercise error paths, basic smoke test of primary features.
- Confirm all menus exist and every menu item works.
- Confirm all buttons are functional and mapped to intended actions.
- Record any failures, blockers, or accessibility/automation limitations.

## Acceptance Criteria
- Each checklist item is executed with results logged (pass/fail/blocker).
- Any blocked items include a clear reason and next step.
- Evidence of menu and button verification is captured in STATUS.md or test notes.

## Testing Notes
- Use manual UI interaction if automation is blocked by System Events/Accessibility.
- Capture app bundle size and note performance (launch time, memory use) if measurable.
- If UI automation is attempted, record the exact error output.

## Button-Functionality Checks
- Verify all toolbar and in-view buttons respond (including project open/close/create, agent actions, and station assignments).
- Verify any destructive actions show confirmation dialogs and handle cancel.
- Verify drag-and-drop (Break Room → Bridge station) works and updates state.

## Catastrophic Warning
- **Do not delete yourself when testing.**
